# Tests use a private in-memory database, never the local backend/data files.
# (config/application.properties overrides only this value from the main file.)
spring.datasource.url=jdbc:h2:mem:newsfeed-test;DB_CLOSE_DELAY=-1
